# Replace YOUR_USER (and YOUR_PASSWORD if needed) with your Postgres credentials.
# Create the database first: createdb glimpse_prep
DATABASE_URL="postgresql://YOUR_USER@localhost:5432/glimpse_prep?schema=public"
